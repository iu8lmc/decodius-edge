// brain.c - // Client LLM + loop di tool-calling (decodium/hamqth/cluster/store) via cJSON+HTTP.
// SCAFFOLD: implementazione da completare (vedi docs/ARCHITETTURA.md).
#include "brain.h"
#include "esp_log.h"
static const char *TAG = "brain";

