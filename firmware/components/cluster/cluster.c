// cluster.c - // Spot DX dal cluster mondiale (dxwatch).
// SCAFFOLD: implementazione da completare (vedi docs/ARCHITETTURA.md).
#include "cluster.h"
#include "esp_log.h"
static const char *TAG = "cluster";

